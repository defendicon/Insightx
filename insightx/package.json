{
  "name": "insightx",
  "version": "0.0.1",
  "description": "ERPNext Analytics Dashboard",
  "dependencies": {
    "frappe": "0.0.1",
    "frappe-charts": "^1.6.2"
  },
  "scripts": {
    "build": "node esbuild --production --apps insightx --run-build-command",
    "dev": "node esbuild --apps insightx --run-build-command",
    "production": "node esbuild --production --apps insightx --run-build-command"
  },
  "esbuild": {
    "entry_points": {
      "insightx": "./insightx/public/js/index.js"
    }
  }
} 
